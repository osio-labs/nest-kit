/** Injection token for the Logger module options. */
export const LOGGER_MODULE_OPTIONS = 'LOGGER_MODULE_OPTIONS';

/** Injection token for the raw Pino logger instance. */
export const PINO_LOGGER = 'PINO_LOGGER';

/** Default minimum log level when none is specified. */
export const DEFAULT_LOG_LEVEL = 'info';

/** Default correlation ID header name. */
export const DEFAULT_CORRELATION_ID_HEADER = 'x-correlation-id';
