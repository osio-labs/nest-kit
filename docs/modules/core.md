# Core

> Shared types, utilities, and base classes used across all other modules.

🚧 **Not yet implemented** — coming in an alpha release.

```ts
import { someUtil } from '@os.io/nest-kit';
// or explicitly:
import { someUtil } from '@os.io/nest-kit/core';
```
